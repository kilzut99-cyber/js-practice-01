// ЗАДАНИЕ 1: Переключение темы

if (localStorage.getItem("theme") === "dark") {
  document.body.classList.add("dark-theme");
}

// 1. Найдите кнопку по id и сохраните в переменную const - при получении ссылки на элемент он начинает реагировать на действия пользователя
const themeButton = document.getElementById("theme-toggle"); //document - обращение к объектной модели наше HTML-страницы; getElementById('theme-toggle')- базовый метод DOM, ещет элемент с идентификатором 'theme-toggle'

// 2. Добавьте слушатель события 'click'- обработчик событий, заставляет кнопку "ожить"
themeButton.addEventListener("click", function () {
  //метод определяет браузеру при клике пользователя на эту кнопку выполнить все действия в фигурных скобках; themeButton - переменная-ссылка на кнопку; addEventListener-метод-слушатель
  // 3. Переключите класс 'dark-theme' у body
  document.body.classList.toggle("dark-theme"); //toggle -переключатель, при отсутсвии добавляет класс dark-theme в body, при наличии - удаляет этот класс

  // ЗАДАНИЕ 3: Работа с переменными (студент дописывает)
  // Подсказка: объявите переменную isDarkMode и проверьте,
  // есть ли у body класс 'dark-theme'
  const isDarkMode = document.body.classList.contains("dark-theme"); // объявляем переменную и проверяем наличие класса; метод contains - проверяет наличие класса и возвращает логическое значение

  // Выведите в консоль тип данных isDarkMode
  // console.log(typeof isDarkMode);

  console.log("Тип:", typeof isDarkMode, "| Значение:", isDarkMode); // в консоли появится: "boolean"- логический тип: если тема включилась в ней лежит true, если выключилась - false

  if (isDarkMode) {
    localStorage.setItem("theme", "dark");
    themeButton.textContent = "Включить светлую тему";
  } else {
    localStorage.setItem("theme", "light");
    themeButton.textContent = "Включить темную тему";
  }
  console.log("Is Dark Theme active?", isDarkMode);
  console.log("Type of item isDarkMode:", typeof isDarkMode);
});

// ЗАДАНИЕ 2: Вывод данных

// 1. Найдите элемент с id="app"
const appDiv = document.getElementById("app"); //const appDiv - поиск контейнера (appDiv хранит ссылку на ); document - обращение к объектной модели наше HTML-страницы; getElementById('app')- базовый метод DOM, ещет элемент с идентификатором 'app'

// 2. Запишите в него текущую дату и время
appDiv.textContent =
  "Текущая дата и время: " + new Date().toLocaleString("ru-RU"); // textContent - отвечает за текст внутри элемента (при присвеонии значения старый текст внутри блока appDiv стирается, а новый (дата и время) появляется); new Date() - оздает объект с текущим временем (прямо в ту миллисекунду, когда сработал код); .toLocaleString('ru-RU') — метод, который превращает «техническую» дату в понятный человеку формат (например, 15.02.2024, 14:30:05)

// Вопрос для размышления: Почему мы используем textContent, а не innerHTML? Это безопасность - innerHTML интерпретирует строку как HTML-код и если в данных окажется вредоносный скрипт (например, из формы ввода или внешнего API), браузер его выполнит. Что касается textContent воспринимает всё строго как простой текст и если кто-то попытается вставить <script>alert('взлом')</script> через textContent, пользователь просто увидит этот текст на экране как буквы, и ничего не произойдет. Производительность - textContent работает быстрее, а  innerHTML заставляет браузер пересобирать DOM-узлы внутри элемента, что требует больше ресурсо. Предсказуемость- когда необходимо просто вывести дату, имя пользователя или результат вычислений, не нужны HTML-теги

/* 
   🌟 УРОВЕНЬ PRO (Extra Challenge):
   1. Добавьте на кнопку иконку (например, 🌙 или ☀️).
   2. Сделайте так, чтобы при включении темной темы иконка менялась на ☀️, 
      а при светлой — на 🌙.
   3. В CSS используйте переменные:
      :root { --bg-color: #ffffff; --text-color: #333; }
      body { background-color: var(--bg-color); color: var(--text-color); }
      body.dark-theme { --bg-color: #2c3e50; --text-color: #ecf0f1; }
*/
